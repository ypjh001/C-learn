#pragma once
#include<iostream>
#include<string>
#include<stack>//栈头文件


using namespace std;


/*
	栈：结构类似n。上面是栈低，下面是栈顶，只有一个出入口，实例：如弹匣，每次只能访问栈顶的数据，所以不能遍历
		存入数据：入栈--push
		弹出数据：出栈--pop

*/

void test_stack();
